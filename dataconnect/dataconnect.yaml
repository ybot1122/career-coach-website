specVersion: "v1"
serviceId: "tobyscoaching-ffcc5-service"
location: "us-east4"
schemas:
  - source: "./schema"
    datasource:
      postgresql:
        database: "tobyscoaching-ffcc5-database"
        cloudSql:
          instanceId: "tobyscoaching-ffcc5-instance"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./connector"]
